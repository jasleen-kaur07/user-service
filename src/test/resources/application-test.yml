git statusspring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${TEST_DB_NAME:user_service_test_db}
    username: ${DB_USERNAME:user_service}
    password: ${DB_PASSWORD:user_service_dev_pw}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    clean-disabled: false
